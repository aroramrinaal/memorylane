import React from 'react';
import MedTracker from '@/Components/MedTracker'; // Import the MedTracker component

const MedTrackerPage: React.FC = () => {
  return (
    <>
      {/* Only render the MedTracker component */}
      <MedTracker />
    </>
  );
};

export default MedTrackerPage;